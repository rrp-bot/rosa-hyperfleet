version: 0.2

env:
  shell: bash

phases:
  install:
    commands:
      - chmod +x ./scripts/bootstrap-argocd.sh

  build:
    on-failure: RETRY-1
    commands:
      - ./scripts/buildspec/bootstrap-argocd-mc.sh
